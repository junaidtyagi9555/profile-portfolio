# Create a temporary directory
mkdir snake-fix
cd snake-fix

# Clone your repository
git clone https://github.com/junaidtyagi9555/profile-portfolio.git
cd junaidtyagi9555

# Create output directory and SVG file
mkdir -p output

# Create a simple snake SVG (temporary)
echo '<svg xmlns="http://www.w3.org/2000/svg" width="800" height="200" viewBox="0 0 800 200">
<rect width="800" height="200" fill="#0d1117"/>
<text x="400" y="100" text-anchor="middle" fill="#00D4AA" font-family="monospace" font-size="20">
  Snake animation will appear here once workflow runs
</text>
</svg>' > output/github-contribution-grid-snake.svg
